#include <utils/hello.h>
#include <utils/sockets.h>

int main(int argc, char* argv[]) {
    saludar("kernel");
    iniciarServidor(puerto, log, io);//Maqueta
    iniciarServidor(puerto, log, cpu);//Maqueta
    crearConexion(ip, puerto, log);//Maqueta

    return 0;
}
